[pytest]
testpaths = tests
pythonpath = src
addopts = -ra --strict-markers
markers =
    unit: unit checks without external network
    integration: checks against test gateway (requires BASE_URL and network)
    smoke: critical happy-path checks
filterwarnings =
    ignore::DeprecationWarning
